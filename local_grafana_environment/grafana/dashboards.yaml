apiVersion: 1

providers:
  - name: NetCore    # A uniquely identifiable name for the provider
    folder: NetCore  # The folder where to place the dashboards
    type: file
    options:
      path:  /var/lib/grafana/dashboards
